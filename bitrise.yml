format_version: "11"
project_type: other

meta:
  bitrise.io:
    project_scanner:
      enabled: false
    stack: linux-docker-android-22.04

workflows:
  validation_workflow_no_ssh:
    steps:
      - script@1:
          title: Dummy step
          inputs:
            - content: |-
                echo "CI pipeline is valid"
